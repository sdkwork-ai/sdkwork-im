[package]
name = "sdkwork-api-im-standalone-gateway"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.8"
tokio = { version = "1.48", features = ["macros", "net", "rt-multi-thread"] }
sdkwork-api-im-assembly = { path = "../sdkwork-api-im-assembly" }
