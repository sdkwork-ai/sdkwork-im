/**
 * Composition metadata pointer for this package.
 *
 * `APP_COMPOSITION_SPEC.md` resolves composition metadata through the core
 * package export, so the path is relative to the package root that owns
 * `specs/component.spec.json`.
 */
export const sdkworkComponentSpecPath: string = '../../../specs/component.spec.json';
