# Sdkwork IM Flutter Mobile — optional local overrides.
# Full stack development uses repository-root topology profiles:
#   pnpm dev

# Application ingress (default from etc/topology/standalone.development.env)
SDKWORK_IM_APPLICATION_PUBLIC_HTTP_URL=http://127.0.0.1:18079
SDKWORK_IAM_APP_API_BASE_URL=http://127.0.0.1:18079
