# scripts

App-local build, validation, generation, migration, and development utilities
for the Sdkwork IM H5 application root.

Scripts here operate within the H5 application boundary. Repository-wide
tooling belongs at the repository root `scripts/` directory.
