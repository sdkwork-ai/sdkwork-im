# SDKWork IM H5 Shell Specs

Machine-readable integration authority: [`component.spec.json`](component.spec.json).

The shell composes enabled H5 capability route contributions and mobile navigation. It owns no
business service, SDK construction, authentication implementation, or capability state.
