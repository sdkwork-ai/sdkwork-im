# SDKWork IM Cloud Gateway Source Configuration

The cloud gateway consumes the root topology profile selected through
`../../../etc/sdkwork.deployment.config.json`. Cloud application ingress and platform API gateway
origins remain distinct. Secrets and installed operator configuration are external to source.
