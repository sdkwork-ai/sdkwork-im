# SDKWork IM Standalone Gateway Source Configuration

The TOML files in this directory are the typed process templates for the four lifecycle
environments. Public application origins are selected by the root
`../../../etc/sdkwork.deployment.config.json`. Secret values are not committed; production
templates reference protected secret files.
