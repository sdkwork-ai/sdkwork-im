# v0.1.0 - Loop 87 - 2026-07-07

Status update: release package-matrix status in this loop is superseded by
`docs/release/2026-07-08-v0.1.0-loop-88.md`. The blocker count below is the
point-in-time state before the 13-target package matrix alignment.

## Summary
- Aligned the PC chat startup flow with the generated IM TypeScript SDK authority by using `chat.inbox.list` for inbox loading.
- Standardized Playwright IM API fixtures to SDKWork v3 success envelopes: `code: 0`, `data.items + data.pageInfo` for list responses, and `data.item` with HTTP `201` for message creation.
- Hardened portal dashboard access so fallback AppContext without `portal.read` permission does not satisfy authenticated portal snapshot access.
- Kept dashboard `dataAvailability=false` for default runtimes without real ops metrics, avoiding fake commercial data claims.
- Added package-local `typescript` tooling for `@sdkwork/im-sdk` so its `typecheck` script runs without relying on global binaries.

## Code
- `sdks/sdkwork-im-sdk/sdkwork-im-sdk-typescript/src/transport-client-like.ts`
  - aligned the transport shape with generated `ChatInboxApi.list`.
- `sdks/sdkwork-im-sdk/sdkwork-im-sdk-typescript/src/conversations-module.ts`
  - delegated inbox listing through `chat.inbox.list`.
- `apps/sdkwork-im-pc/packages/sdkwork-im-pc-chat/src/services/ChatService.ts`
  - consumed `chat.inbox.list` for paged chat list loading.
- `apps/sdkwork-im-pc/packages/sdkwork-im-console-communications/src/services/GroupService.ts`
  - consumed `chat.inbox.list` for group inbox projection reads.
- `apps/sdkwork-im-pc/e2e/fixtures/*.ts`
  - replaced legacy `requestId`, string `code`, and bare list bodies with SDKWork v3 envelopes.
- `services/portal-service/src/handlers.rs`
  - required `portal.read` permission for authenticated portal snapshots.
- `services/portal-service/tests/http_smoke_test.rs`
  - aligned dashboard data availability expectations with fail-closed default runtime evidence.
- `sdks/sdkwork-im-sdk/sdkwork-im-sdk-typescript/package.json`
  - added package-local `typescript` dev dependency through the workspace catalog.

## Verification
- `pnpm.cmd --dir apps\sdkwork-im-pc run lint` = passed.
- `pnpm.cmd --dir apps\sdkwork-im-pc run build` = passed.
- `node scripts\dev\sdkwork-im-pc-playwright-e2e.test.mjs` = 4 passed.
- `cargo test -p portal-service --test http_smoke_test -- --nocapture` = 4 passed.
- `cargo fmt -p portal-service -p sdkwork-routes-im-portal-app-api` = passed.
- `node ..\sdkwork-specs\tools\check-api-response-envelope.mjs --workspace .` = passed.
- `node ..\sdkwork-specs\tools\check-api-operation-patterns.mjs --workspace .` = passed.
- `node ..\sdkwork-specs\tools\check-pagination.mjs --workspace .` = passed.
- `node ..\sdkwork-specs\tools\check-app-sdk-consumer-imports.mjs --workspace .` = passed.
- `node sdks\sdkwork-im-sdk\bin\verify-sdk.mjs` = passed.
- `pnpm.cmd --dir sdks\sdkwork-im-sdk\sdkwork-im-sdk-typescript run typecheck` = passed.
- `pnpm.cmd run check:commercial-readiness` = implementation checks reached app release evidence assessment; blocked by real release evidence gaps listed below.

## Blockers
- `sdkwork.app.config.json` still has four enabled direct distribution packages with missing SHA-256 checksums.
- The same four packages have no signing evidence while `security.signatureRequired=true`.
- The same four packages have no SBOM evidence while `security.sbomRequired=true`.
- The same four packages have no provenance or attestation evidence while `security.sbomRequired=true`.
- `media.icons.primary`, `media.screenshots[0]`, and `media.previews[0]` still carry `metadata.generatedPlaceholder=true`.
- These blockers must be closed with real release artifacts and real catalog media. They must not be replaced with placeholder strings, inferred checksums, fake signatures, fake SBOMs, or documentation-only claims.

## Tooling Gap
- `cargo fmt --all` is currently blocked by an external workspace layout issue: `<workspace-root>/sdkwork-course/crates/sdkwork-course-gateway-assembly/Cargo.toml` is missing while referenced by sibling workspace metadata.
- Package-level Rust formatting for the touched crates was used instead and passed.
- Several ad-hoc TS service contract scripts under `scripts/dev/*.test.ts` cannot currently be run with plain `tsx` because the PC TypeScript config maps React runtime packages to `.d.ts` files to preserve cross-workspace type identity for `tsc`; removing those mappings breaks PC lint through duplicate React types and missing JSX runtime resolution in sibling packages.
- The supported PC verification path remains `pnpm.cmd --dir apps\sdkwork-im-pc run lint`, build, Playwright e2e, and SDKWork standard checks until a dedicated script-test tsconfig or runner is introduced.

## Release Status
- SDKWork IM is still not commercially releasable as of this loop.
- Implementation and standards checks covered in this loop pass, but commercial sign-off remains blocked until release evidence and media evidence are real and reproducible.
