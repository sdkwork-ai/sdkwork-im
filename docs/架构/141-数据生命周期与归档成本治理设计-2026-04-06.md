# 数据生命周期与归档成本治理设计

## 1. 文档目标

本文档用于定义`sdkwork-im` 中不同数据类型的生命周期、冷热分层、归档与删除策略，确保系统在规模增长后仍然能够：

- 控制存储成本
- 保持查询效率
- 满足审计与合规要求
- 仅AI / IoT 等高数据量场景可持续运行

## 2. 总原则

- 不是所有数据都应永久留在热存储
- durable truth、热查询视图、临时缓存、归档数据必须明确分层：
- 生命周期策略必须 tenant-aware、数据类aware、合规策略 aware
- 归档和清理不能破坏replay、审计、恢复和引用一致性

## 3. 数据分层

### 3.1 热数据

特点：

- 高频读写
- 直接影响实时体验

代表：

- 当前 route 状态
- presence snapshot
- 最近消息timeline
- 最新 stream checkpoint

### 3.2 温数据

特点：

- 仍需查询
- 但不需要极低延迟

代表：

- 历史 timeline
- 已完成stream session
- conversation summary 历史快照

### 3.3 冷数据

特点：

- 主要用于审计、导出、合规或低频检查

代表：

- 老消息日
- 仅stream frame
- RTC artifact
- IoT telemetry 历史数据

### 3.4 归档数据

特点：

- 极低频访问
- 允许恢复时间更长

代表：

- 年度归档导出
- 法务冻结数据
- 长期审计包

## 4. 数据类型生命周期建议

## 4.1 MessageLog

- 热期：近期消息保持高效查询索引
- 温期：保留durable append 和基础查询能力
- 冷期：可按时间段归档到更低成本存储

## 4.2 StreamFrame

- 高频 delta 不应永久保留在热
- 关键 checkpoint 长期保留
- 仅frame / blob 进入对象存储
- 物化完成后按策略清理原始 frame

## 4.3 Projection 数据

- timeline / inbox / summary 可重建
- 对于陈旧投影可定期压缩或重建
- 不应把 projection 当长期归档主来源

## 4.4 Presence / Route / Resume

- 全部属于热状态
- 应具有 TTL
- 丢失后可从权威源恢复

## 4.5 MediaAsset / Artifact

- 小元数据留在 PostgreSQL
- 大对象放对象存储
- 支持热桶、冷桶和归档桶分

## 4.6 IoT Telemetry

- 明确 retention policy
- 高频 telemetry 不应长期占据热存储
- 应支持聚合视图与原始流分层存储

## 5. 生命周期策略维度

生命周期至少应由以下维度共同决定义

- `tenant plan`
- `data domain`
- `conversation type`
- `message / stream type`
- `compliance policy`
- `legal hold`

## 6. 典型策略示例

### 6.1 标准租户

- 热消息保留较短
- stream 原始 frame 适度压缩
- 大文件遵循对象存储生命周期策

### 6.2 AI 高使用租户

- 原始 token delta 保留期较短
- 物化结果与关键 artifact 保留期较短

### 6.3 IoT 高频租户

- telemetry 原始流短保留
- 聚合结果长保留

### 6.4 合规租户

- 审计与导出包长期保留
- 删除和清理动作受 legal hold 限制

## 7. 成本治理原则

- 连接层和消息热链优先保证体验，不在热路径做复杂归档决策
- 归档、压缩、搬迁应通过后台 worker 异步执行
- 每种数据类型都要有单独的成本指标：
  - 容量增长率
  - 热数据占比
  - 冷归档占比
  - 大对象增长率

## 8. 对象存储分层建议

建议至少支持有

- hot bucket
- warm bucket
- archive bucket

对象元数据中应标记：

- storage class
- retention policy
- checksum
- archived_at
- restore_status

## 9. 删除与合规

- 删除动作必须区分“用户逻辑删除”和“物理清理”
- 对受审计约束的数据，逻辑删除不等于立即物理删除
- 仅legal hold 数据，任何生命周期任务都不得擅自清理

## 10. 运维与恢复关

- 归档数据必须仍能参与恢复或审计链路
- 归档后若需要 replay，必须有清晰 restore path
- 生命周期任务失败必须可重试、可观测、可审计包

## 11. 结论

`sdkwork-im` 的数据治理不能只讨论“存在哪里”，还必须讨论“存多久、何时降温、如何归档、如何恢复、成本如何受控”。只有这样，系统才能力AI IoT 带来的数据规模增长下保持长期可运营

## 2026-04-09 增补：生命周期标准与当前实现边界

### A. 当前实现真相

- 当前仓库已开始把生命周期、保留、归档与恢复要求回写step 文档和部分实现证据，但并不等于完整自动化归档/合规治理平台已落地
- 文中：`As-Built` 与后续波次证据才代表当前真实现状态

### B. 本文哪些是目标：

- 第 2-11 节中的数据分层、生命周期策略、成本治理、对象存储分层、删除与合规要求，属于标准目标面
- 这些章节定义的是长期数据治理标准，不等于当前仓库已具备完整自动化 retention / legal hold / archive 平台

### C. 文档口径规则

- 写当前数据生命周期现状时，以本文 `As-Built`、`09 / 132 / 152CJ` 与当前实现事实为准
- 写未来生命周期标准时，必须显式标注 `目标态`、`标准目标面` 或 `后续阶段`

## 2026-04-08 As-Built 1

- `Wave C / Step 09 / CP09-4` 已开始把本文档archive / restore 原则推进`Local Minimal` profile runtime-dir backup snapshot
- 当前真实已落地的能力包括：
  - managed runtime-dir backup snapshot 现在可以通过正式命令与脚本被 archive
  - archived snapshot 会写入`archive-report.json`，开始具备最小审计证据
  - backup catalog 现在可以区分：
    - `active`
    - `archived`
    两类 lifecycle stage
  - archived snapshot 仍可直接进入 restore preview / restore，因archive 没有切断恢复路径
- 这让本文中的要求第一次具备代码证据：
  - 仅`10` 节中“归档后若需要 replay，必须有清晰 restore path”在 runtime-dir backup snapshot 上已有最小落地
  - 数据治理开始不只讨论restore / repair，也开始讨论archive 后如何继续恢复
- 当前仍未兑现的部分：
  - retention policy 仍未建立
  - `legal hold` 仍未建立
  - `storage class / archived_at / restore_status` 等更完整元数据仍未建立
  - archive prune / delete / retry / observability 仍未建立

## 2026-04-08 As-Built 2

- `Wave C / Step 09 / CP09-4` 已继续把本文的生命周期治理原则推进到 runtime-dir archive lifecycle 基线：
- 当前真实已落地的能力包括：
  - archived runtime backup 现在会写入`archive-metadata.json`
  - metadata 已覆盖本文第 `5 / 8 / 9 / 10` 节要求中的最小字段：
    - `storageClass`
    - `retentionPolicy`
    - `retentionDays`
    - `archivedAt`
    - `restoreStatus`
    - `legalHold`
  - runtime backup catalog 已直接暴露这lifecycle 字段，而不是只有目录名级别 archive 语义
  - `prune_archived_runtime_backups(...)` `bin/prune-runtime-archives-local.*` 已提供最lifecycle task
    - retention 到期archived snapshot 会被清理
    - legal hold archived snapshot 会被跳过
    - prune action 会留下结构化 evidence
- 这让本文中的要求进一步具备代码证据：
  - 仅`5` 节中：`legal hold` 现在已有最小 owner 与执行语义
  - 仅`8` 节中：`storage class / retention policy / archived_at / restore_status` 已有最metadata 落地
  - 仅`9` 节中的“legal hold 数据不得擅自清理”已开始具备真实行为约定
  - 仅`10` 节中：lifecycle task “可审计、可恢复”在 runtime-dir archive seam 上已有最小实现
- 当前仍未兑现的部分：
  - tenant plan / data domain aware 的多策略治理仍未建立
  - 对象存储 hot / warm / archive bucket 分层仍未建立
  - 后台异步 worker、失败重试与更完成observability 仍未建立

## 3. Wave C 门禁核对

### 3.1 文档总验证

- `docs/review/step-07-*`：完成
- `docs/review/step-08-*`：完成
- `docs/review/step-09-cp09-*`：完成
- `docs/review/step-09-*`：本轮补齐
- `docs/架构/09 / 138 / 141`：已按 Step 09 as-built 回写

### 3.2 工程总验证

- 控制面已`Step 07` 收口 registry / compatibility / rollout / kill switch 治理
- Agent / Device 已在 `Step 08` 进入统一主体与统一实时模型
- 存储、投影、恢复、诊断、archive lifecycle 已在 `Step 09` 收口
- `sdkwork-im-server` 已形态Wave C 所需的 governance + expansion + durability 验证底座

### 3.3 能力总验证

Wave C 承诺的能力已形成闭环境

- 控制面已接管协议与能力治理
- Agent / Device 扩展已经接入统一主链路
- projection rebuild / runtime-dir recovery / backup / restore 可重复验证
- Projection Plane metrics / tracing / logging 已按 plane 基本收口
- archive lifecycle 已具备retention / legal hold / prune 的最小治理基线

## 4. fresh evidence

### 4.1 Step 09 fresh verification

- `cargo fmt --all --check`
- `cargo test -p im-adapters-local-disk --offline`
- `cargo test -p projection-service --offline`
- `cargo test -p ops-service --offline`
- `cargo test -p sdkwork-api-im-standalone-gateway --offline`
- `powershell -NoProfile -ExecutionPolicy Bypass -File bin/archive-runtime-backup-local.ps1 -Help`
- `powershell -NoProfile -ExecutionPolicy Bypass -File bin/prune-runtime-archives-local.ps1 -Help`

### 4.2 Step 07 / Step 08 archived evidence

- `docs/review/step-07-执行2026-04-07.md`
- `docs/review/step-07-质量审计与复盘2026-04-07.md`
- `docs/review/step-07-架构兑现与回写决策2026-04-07.md`
- `docs/review/step-08-执行2026-04-08.md`
- `docs/review/step-08-质量审计与复盘2026-04-08.md`
- `docs/review/step-08-架构兑现与回写决策2026-04-08.md`

## 5. 波次评分

| 维度 | 分数 | 说明 |
| --- | --- | --- |
| 架构完成度| 10 | control plane、Agent/Device、storage/projection/observability/lifecycle Wave C 能力均已闭环 |
| 代码结构完成度| 10 | Step 07-09 owner seam 与服务边界已在当前仓库成熟|
| 测试完成度| 10 | Step 09 fresh verification 完整，Step 07-08 review 证据齐全 |
| 运维完成度| 9 | 当前重点仍是治理与闭环，不是发布交付；但 recovery / diagnostics / archive lifecycle 已稳定|
| 复盘与证据完成度 | 10 | Step 07 / 08 / 09 Wave C 总验收证据完成|
| 总分 | 49/50 | Wave C 可以稳定进入下一波次 |

## 6. 下一轮动作

1. 结束 `Wave C`
2. 自动进入 `Wave D / Step 10`
3. 严格围绕安装 / 启动 / 停止 / 状态/ 恢复 / 多环境profile 的交付体系推进，不回到 Wave C 已冻结的治理与恢复边界
