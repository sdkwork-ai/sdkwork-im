> Migrated from `docs/review/step-11-cp11-2-连接消息流量化基线-执行卡-2026-04-08.md` on 2026-06-24.
> Owner: SDKWork maintainers

# Step 11 / CP11-2 连接消息流量化基线 执行卡 - 2026-04-08

## 当前上下文
- 当前波次：`Wave D`
- 当前 step：`Step 11`
- 当前子任务：`CP11-2`
- 前置状态：
  - `CP11-1` 已完成，Step 11 已有统一场景清单与 operator 文档
  - 当前真实缺口收敛到：必须拿出至少一轮 `connection / message / stream` 的定量结果

## 本轮为什么做这个增量
- `docs/step/11-性能高可用与灾备演练.md` 明确要求 `CP11-2` 完成至少一轮连接、消息、流三类压测。
- 在当前仓库里，最优做法不是引入新的 benchmark 框架，而是先把 `standalone.split-services.development` 的最小可重复量化入口固化下来，再用它产出第一轮可信结果。

## 本轮实际完成

### 1. 已建立可重复执行的本地量化基线
- 新增：`tools/perf/step-11-cp11-2-local-baseline.json`
- 当前冻结参数：
  - `connectionCount = 32`
  - `messageCount = 64`
  - `frameCount = 64`
- 基线层级：
  - `profile = standalone.split-services.development`
  - `tier = CI Smoke Tier`

### 2. 已新增真实量化执行门禁
- 新增：`crates/sdkwork-api-im-standalone-gateway/tests/performance_quant_baseline_test.rs`
- 覆盖：
  - websocket 连接量化
  - HTTP 消息写入量化
  - stream frame 追加量化
- 每个场景都会打印一条 `STEP11_METRIC {json}` 指标行，便于 review 文档直接回填结果

### 3. 已把量化基线接入既有 Step 11 文档与 catalog
- `docs/部署/性能与灾备演练场景.md`
  - 新增 `tools/perf/step-11-cp11-2-local-baseline.json`
  - 新增 `crates/sdkwork-api-im-standalone-gateway/tests/performance_quant_baseline_test.rs`
- `tools/perf/step-11-scenario-catalog.json`
  - `connection / message / stream` 三类场景已从 `catalog-frozen` 推进到 `measured-local-baseline`

### 4. 已完成第一轮定量结果
- `connection`
  - `32 / 32` 成功
  - `19.789 ms` 总时长
  - `17.216 ms` `connectP95Ms`
  - `1617.068 connections/s`
- `message`
  - `64 / 64` 成功并落到 conversation summary
  - `10.459 ms` 总时长
  - `0.225 ms` `postP95Ms`
  - `6119.132 message/s`
- `stream`
  - `64 / 64` 成功并持久化
  - `5.714 ms` 总时长
  - `0.112 ms` `appendP95Ms`
  - `11201.344 frames/s`

## TDD / Red-Green 证据

### Red
- `cargo test -p sdkwork-api-im-standalone-gateway --offline --test performance_quant_baseline_test`
  - 初始失败：缺少 `tools/perf/step-11-cp11-2-local-baseline.json`

### Green
- 补齐本地量化基线配置、文档接线与量化测试后，同一套测试 fresh 保持通过
- 并通过 `-- --nocapture` 输出真实量化结果，不是静态文档填数

## Fresh 验证
- `cargo test -p sdkwork-api-im-standalone-gateway --offline --test performance_quant_baseline_test -- --nocapture`
- `cargo fmt --all --check`

## 当前判断
- `CP11-2`：闭环
- 已兑现：
  - 至少一轮 `connection / message / stream` 的定量结果
  - 本地最小量化入口已可重复执行
  - Step 11 的性能结论开始进入 `docs/review`
- 当前仍未兑现：
  - `CP11-3` 的 `drain / rebalance / restore / failover`
  - `CP11-4` 的整步量化报告收口
  - 预发布层与容量层的更高规模结果

## 下一轮继续做什么
1. 进入 `CP11-3`
2. 基于同一 `standalone.split-services.development` 基线完成 `drain / rebalance / restore`，并决定 `failover` 的最小可信演练路径
3. 继续把演练结果写进 `docs/review/` 和 `docs/架构/`

