> Migrated from `docs/review/S10-执行卡-2026-04-10.md` on 2026-06-24.
> Owner: SDKWork maintainers

# S10 执行卡 - 2026-04-10

## 1. 架构对齐
- 对齐能力：`stream lifecycle / rtc lifecycle / media artifact lifecycle / provider selection / recording playback`
- 对齐文档：
  - `docs/step/100-面向DDD的IM重构升级总实施计划-2026-04-09.md`
  - `docs/架构/150-插件化提供商体系与设备接入设计-2026-04-08.md`
  - `docs/架构/150A-对象存储插件运行时与媒体下载面设计-2026-04-08.md`
  - `docs/架构/150B-RTC录制对象存储运行时与播放面设计-2026-04-08.md`
  - `docs/架构/152CJ-current-architecture-as-built-alignment-2026-04-09.md`
- 本轮目标不是预设补代码，而是先基于仓库真相判断：`S10` 现有实现是否已足够闭环。
- 若 fresh evidence 证明存在真实缺口，再退回 `测试先红 -> 最小实现 -> 回写`。

## 2. Current / Target / Deferred
- Current State：
  - `streaming-service` 已具备 `open / append / list / checkpoint / complete / abort` 独立面，并对 conversation scope 保持 gateway guard。
  - `im-call-runtime` 已具备 `create / invite / accept / reject / end / signals / credentials / provider-callback / provider-health / recording-artifact`。
  - `media-service` 已具备 `upload create / complete / get / download-url / provider-health`，并通过对象存储 provider 选择签发下载地址。
  - `sdkwork-im-server` 已承接 conversation-bound stream/rtc/media 装配、访问控制、realtime fanout 与 runtime 持久化恢复。
- Target State：
  - 以 fresh verification 证明 `CPR10-1/2/3` 已闭环，并把 `S10` 收口到 `step_closure`。
- Deferred：
  - `S11` 的 agent/automation actor 扩展。
  - `S12` 的 AIoT/external access 扩展。
  - `S13` 的 perf/HA/DR/retention 演练与发布级 evidence。

## 3. 本轮目标 / 非目标
- 目标：
  - 冻结 `S10` 证据口径与 CPR。
  - fresh 验证 standalone 与 assembled 两套主链。
  - 基于证据决定 `S10 = step_closure` 或 `S10 = 仍有单一主缺口`。
- 非目标：
  - 不凭想象扩写不存在的 provider matrix 能力。
  - 不把 `S11/S12/S13` 的 future owner 混入 `S10`。
  - 不因为仓库已有大量脏改动而回退无关文件。

## 4. 主写入范围
- 文档：
  - `docs/review/S10-*`
  - `docs/架构/152CJ-*`
  - `docs/release/*`
- 条件写入代码：
  - 仅当 fresh verification 证明 `S10` 存在单一真实缺口时，才允许改 `services/streaming-service`、`services/im-call-runtime`、`services/media-service`、`crates/sdkwork-api-im-standalone-gateway` 及对应测试。
- 禁止并写：
  - `docs/step/100-*`
  - `docs/step/101-*`
  - 无关 social / space / runtime 聚合

## 5. 最小交付包
- 执行卡冻结。
- fresh verification：
  - standalone `streaming-service / im-call-runtime / media-service`
  - assembled `sdkwork-im-server` 的 stream/rtc/media 关键集成链路
- review 结论：
  - `S10-质量审计与复盘`
  - `S10-架构兑现与回写决议`
- backwrite：
  - `152CJ` 主文或 Loop 补充
  - `CHANGELOG`
  - `2026-04-10-v0.0.20-loop-20.md`

## 6. 能力覆盖声明
- 本轮要求覆盖：
  - `CPR10-1`：`stream.open / delta / checkpoint / complete / abort`，以及 conversation-bound assembled path
  - `CPR10-2`：`rtc.create / invite / accept / reject / end / signals / credentials / callback / provider-health / recording-artifact`
  - `CPR10-3`：`media upload create / complete / get / download-url / provider-health` 与 object storage provider 选择
- 本轮显式后延：
  - 多厂商真实联邦演练与 failover drill：`S13`
  - Agent/Device 专用流与外部协作扩展：`S11/S12`

## 7. 测试与验证
- 自动化：
  - `cargo test -p streaming-service --offline --tests`
  - `cargo test -p im-call-runtime --offline --tests`
  - `cargo test -p media-service --offline --tests`
  - `cargo test -p sdkwork-api-im-standalone-gateway --offline --test stream_runtime_persistence_test`
  - `cargo test -p sdkwork-api-im-standalone-gateway --offline --test rtc_runtime_persistence_test`
  - `cargo test -p sdkwork-api-im-standalone-gateway --offline --test media_provider_http_test`
  - `cargo test -p sdkwork-api-im-standalone-gateway --offline --test http_e2e_test`
  - `cargo test -p sdkwork-api-im-standalone-gateway --offline --test access_control_e2e_test`
- 主证据：
  - `services/streaming-service/src/lib.rs`
  - `services/im-call-runtime/src/lib.rs`
  - `services/media-service/src/lib.rs`
  - `services/streaming-service/tests/stream_lifecycle_test.rs`
  - `services/im-call-runtime/tests/rtc_signal_flow_test.rs`
  - `services/media-service/tests/media_asset_test.rs`
- 辅证据：
  - `crates/sdkwork-api-im-standalone-gateway/tests/stream_runtime_persistence_test.rs`
  - `crates/sdkwork-api-im-standalone-gateway/tests/rtc_runtime_persistence_test.rs`
  - `crates/sdkwork-api-im-standalone-gateway/tests/media_provider_http_test.rs`
  - `crates/sdkwork-api-im-standalone-gateway/tests/http_e2e_test.rs`
  - `crates/sdkwork-api-im-standalone-gateway/tests/access_control_e2e_test.rs`
- fresh 结果：
  - `streaming-service`：`13 passed`
  - `im-call-runtime`：`19 passed`
  - `media-service`：`13 passed`
  - `sdkwork-im-server/stream_runtime_persistence_test`：`1 passed`
  - `sdkwork-im-server/rtc_runtime_persistence_test`：`1 passed`
  - `sdkwork-im-server/media_provider_http_test`：`2 passed`
  - `sdkwork-im-server/http_e2e_test`：`36 passed`
  - `sdkwork-im-server/access_control_e2e_test`：`33 passed`

## 8. 架构回写预判
- 当前结论：`实现更具体`
- 原因：
  - `152CJ` 需要把 `stream / rtc / media` 从“模块存在”提升为“能力已验证的 as-built”。
  - 当前实现还显式澄清了 ownership：standalone service 保持 lifecycle/provider surface，conversation-bound assembled binding 归 `sdkwork-im-server`。

## 9. 迁移 / 降级 / 回退
- 数据迁移：默认无；除非 fresh verification 暴露缺口并进入代码修复。
- 降级路径：
  - 若 standalone 通过但 assembled 失败，`S10` 保持 `not_closed`，assembled gap 优先修。
  - 若 assembled 通过但 standalone surface 漂移，`S10` 同样不得收口。
- 回退条件：
  - 任一 `CPR10-*` 缺主证据或缺 fresh test 结果。
  - `152CJ`、`review`、`release` 未同步。

## 10. 检查点
- `CPR10-1`：stream 生命周期闭环。
- `CPR10-2`：RTC provider/runtime surface 闭环。
- `CPR10-3`：media artifact/object-storage surface 闭环。

## 11. 下一步准入
- 若本轮 `CPR10-1/2/3` 全部成立：允许进入 `S11 || S12`。
- 若本轮存在单一真实缺口：继续停留 `S10`，先做该缺口的 TDD 补齐。
- Deferred 归属：
  - `S11`：agent/automation
  - `S12`：device/external
  - `S13`：perf/HA/DR/retention evidence

## 12. 出口 6 问
- Q1 `95` 是否已判定本 step 对应能力闭环，而非局部补丁：`yes`
- Q2 `97` 是否已写清本轮已兑现与 deferred 的架构结论：`yes`
- Q3 是否已给出“无需回写 / 实现更具体 / 实现有偏移”三选一：`yes`
- Q4 未完成能力是否都已归属到后续 `Sxx` 或 owner：`yes`
- Q5 是否具备 `1` 个主证据和 `1` 组辅证据：`yes`
- Q6 回退、降级、阻塞升级条件是否已写入并经 review：`yes`
- 结论：`S10 = step_closure`

