> Migrated from `docs/架构/25-实现Review与修复计划-2026-04-05.md` on 2026-06-24.
> Owner: SDKWork maintainers

# 实现 Review 与修复计划（2026-04-05）

## 1. 本轮 review 范围

本轮重点核对以下区域是否符合当前 `docs/架构` 已冻结标准：

- 会话成员读写权限
- 时间线 / 会话摘要 / 成员列表读取权限
- 通知可见性隔离
- 多租户与当前认证主体边界

## 2. 已确认并修复的问题

### 2.1 高危：非成员可对会话执行写操作

问题表现：

- 非成员此前可以调用会话写路径，向任意已知 `conversationId` 发消息
- 非成员此前可以对任意已知 `messageId` 执行编辑和撤回

根因：

- `conversation-runtime` 在 `post_message / edit_message / recall_message` 中只尝试回填 `sender.memberId`
- 运行时没有强制要求当前操作者必须是会话有效成员

修复：

- 新增 `RuntimeError::PermissionDenied`
- 在 `conversation-runtime` 内统一校验 active member
- `add_member / remove_member / read_cursor_view / post_message / edit_message / recall_message` 全部接入成员校验

### 2.2 高危：非成员可读取私有会话视图

问题表现：

- 同租户但非会话成员此前可以读取：
  - 会话成员列表
  - 时间线
  - 会话摘要

根因：

- `sdkwork-im-server` 的 `list_members / get_timeline / get_conversation_summary / get_read_cursor`
- `projection-service` 的 `get_timeline / get_conversation_summary / get_read_cursor`
- 都缺少当前认证主体是否为会话 active member 的判断

修复：

- `conversation-runtime` 新增 `require_active_member(...)`
- `sdkwork-im-server` 新增 `ensure_conversation_member(...)`
- `projection-service` 新增 `is_active_member(...)` 和统一的访问校验

### 2.3 高危：通知查询未按 recipient 隔离

问题表现：

- 同租户下任意主体此前都可以查询和读取其他人的通知

根因：

- `notification-service` 的 `list_notifications(...)` 只按 `tenant` 过滤
- `get_notification(...)` 只按 `tenant + notificationId` 命中
- 未校验 `recipient_id == auth.actor_id`

修复：

- `list_notifications(...)` 改为 tenant + recipient 双重过滤
- `get_notification(...)` 改为只允许 recipient 自身读取
- 非 recipient 读取统一返回 not found，避免扩大枚举面

## 3. 本轮新增验证

- `services/conversation-runtime/tests/conversation_flow_test.rs`
  - 新增非成员不能发消息
  - 新增非成员不能编辑/撤回消息
- `crates/sdkwork-api-im-standalone-gateway/tests/access_control_e2e_test.rs`
  - 新增非成员不能读取私有会话成员/时间线/摘要
- `services/notification-service/tests/http_smoke_test.rs`
  - 新增 recipient 隔离校验
- `services/projection-service/tests/http_smoke_test.rs`
  - 新增成员读权限前置条件和非成员 403 校验

## 4. 本轮验证结果

已执行并通过：

- `cargo fmt --all`
- `cargo test -p conversation-runtime --offline`
- `cargo test -p projection-service --offline`
- `cargo test -p notification-service --offline`
- `cargo test -p sdkwork-api-im-standalone-gateway --offline`
- `cargo test --workspace --offline`

## 5. 尚未关闭的风险

### 5.1 中高风险：消息编辑/撤回缺少“消息所有者或管理角色”校验

当前状态：

- 已阻止非成员越权
- 但仍未冻结“成员是否只能编辑/撤回自己消息，还是 owner/admin 可以代操作”的授权标准

下一步建议：

- 在 `docs/架构/06-Gateway-API-与协议设计.md` 和消息变更标准中冻结规则
- 然后补 runtime 授权校验和回归测试

### 5.2 中风险：本地最小 profile 仍接受 trusted headers 作为认证来源

当前状态：

- `im-auth-context` 仍允许无 `Authorization` 时退回 `x-tenant-id / x-user-id`
- 这对内部服务和测试方便，但对直接暴露的 app profile 不够收敛

下一步建议：

- 为外部入口增加 strict auth mode
- `sdkwork-im-server` 默认要求 `Authorization: Bearer ...`
- 内部服务或测试场景显式启用 trusted identity mode

### 5.3 中风险：消息提交后的通知 recipient 选择仍较弱

当前状态：

- 现在通知隔离正确
- 但 `sdkwork-im-server` 在 `message.posted` 后仍默认给当前发送者自己创建通知

下一步建议：

- 根据会话成员关系，冻结“消息通知发给谁、是否排除发送者、是否按设备/在线态分流”的标准
- 再改 side-effect 编排实现

## 6. 下一轮计划

1. 冻结消息编辑/撤回授权标准，补 owner/admin/sender 权限矩阵。
2. 拆分外部入口认证模式，收敛 `trusted headers` 只用于内部可信链路。
3. 补消息通知 recipient 策略，避免当前仅给发送者自己发通知。
4. 继续 review 自动化、审计、RTC、streaming 的权限边界和租户隔离。

## 7. Review Cycle 2 实际完成结果

### 7.1 重要：活跃成员仍可编辑或撤回他人消息

问题表现：

- 上一轮只补了“必须是 active member”，但未补“是否是消息所有者或管理角色”的矩阵。
- 结果是任意群成员仍可编辑别人消息，任意单聊成员仍可撤回对端消息。

根因：

- `conversation-runtime::edit_message` 与 `conversation-runtime::recall_message` 只校验 active member。
- 运行时未区分 `group / direct` 会话类型，也未利用成员角色做撤回治理判定。

本轮标准与修复：

- 新增 `docs/架构/26-消息变更授权与通知收件人标准-2026-04-05.md`
- 编辑：仅原发送者可编辑
- 撤回：
  - 原发送者可撤回自己消息
  - `group` 中 `owner / admin` 可撤回他人消息
  - `direct` 中不允许 `owner / admin` 覆盖撤回对端消息
- `conversation-runtime` 新增会话类型感知，并按上述矩阵执行业务校验

新增验证：

- `test_member_cannot_edit_or_recall_other_members_message`
- `test_group_owner_can_recall_but_not_edit_other_members_message`
- `test_direct_conversation_owner_cannot_recall_other_members_message`

### 7.2 重要：message.posted 仍只给发送者自己创建通知

问题表现：

- `sdkwork-im-server` 在 `message.posted` side-effect 中把 `recipient_id` 固定为 `auth.actor_id`
- 结果是消息发送者自己收到站内通知，其他会话成员收不到消息通知

根因：

- 通知 side-effect 未按 active members 推导收件人
- 多收件人场景也没有为 `notification_id` 建立按 recipient 的唯一性

本轮标准与修复：

- `message.posted` 的通知收件人来自 active members
- 默认排除发送者自身
- 每个收件人生成独立通知任务，`notification_id` 包含 `message_id + recipient_id`
- 当前仍保持同步 `requested -> dispatched` 最小流水

新增验证：

- `test_local_minimal_profile_fanouts_message_notifications_to_other_active_members_only`
- `test_local_minimal_profile_exposes_notification_automation_audit_and_ops_capabilities` 已按新标准更新

## 8. 当前剩余风险

1. 外部入口仍允许 trusted headers fallback，尚未完成 strict bearer-only 收敛。
2. 通知收件人虽然已按成员 fanout，但仍未按设备在线态、免打扰、多通道策略分流。
3. 群治理撤回尚未单独产出 moderation / policy 事件，后续需要与审计模型进一步对齐。

## 9. 下一轮计划更新

1. 收敛 `im-auth-context` 的外部 strict auth 模式，并替换 app-facing 入口默认解析器。
2. 继续 review `automation-service / audit-service / im-call-runtime / streaming-service` 的授权边界。
3. 为群治理撤回补充审计与治理事件标准，避免后续 moderation 能力与消息事件混杂。
4. 冻结成员治理与 direct 会话成员边界标准，补足 group 角色矩阵与 direct 二人边界。

## 10. Review Cycle 3 实际完成结果

### 10.1 重要：实际主程序入口仍接受 trusted headers fallback

问题表现：

- `crates/sdkwork-api-im-standalone-gateway/src/main.rs` 之前直接启动 `build_default_app()`
- `build_default_app()` 内所有 handler 仍使用 `resolve_auth_context(...)`
- 因此当前可部署版本本身就接受 `x-tenant-id / x-user-id` 作为公网入口认证来源

根因：

- trusted headers 原本是为了测试装配和内部可信链路保留
- 但代码层没有把“测试/内部 builder”和“外部 public builder”拆开
- 结果是宽松 builder 被主程序直接复用

本轮修复：

- `im-auth-context` 新增 `resolve_bearer_auth_context(...)`
- `sdkwork-im-server` 新增 `build_public_app()`
- `build_public_app()` 在 health 之外统一要求 Bearer
- `main.rs` 改为启动 `build_public_app()`
- `build_default_app()` 暂保留给测试与内部装配，避免一次性破坏全工作区离线测试基线

新增验证：

- `crates/im-auth-context/tests/auth_context_test.rs`
  - Bearer-only 解析入口拒绝 trusted headers fallback
- `crates/sdkwork-api-im-standalone-gateway/tests/public_auth_e2e_test.rs`
  - public app 拒绝 trusted headers-only 请求
  - public app 接受 Bearer 请求

## 11. 当前剩余风险

1. 当前只收敛了 `sdkwork-im-server` 真实主入口；其它可独立部署服务的 `main.rs` 仍需逐个 review。
2. `build_default_app()` 仍是宽松模式，后续应评估是否重命名为更明确的 internal/test builder，降低误用概率。
3. WebSocket、RTC、streaming 外部入口虽已被 public app middleware 覆盖，但仍缺少更细颗粒度的 strict auth 契约测试。

## 12. 下一轮计划更新

1. 继续 review `session-gateway / im-call-runtime / streaming-service / control-plane-api` 的实际部署入口，判断是否也需要 public strict builder。
2. 为 WebSocket 握手和 RTC/streaming 外部入口补 Bearer-only 契约测试。
3. 评估把默认宽松 builder 改名为 `build_internal_app()` 的迁移成本，减少部署误用。

## 13. Review Cycle 4 实际完成结果

### 13.1 高风险：成员模型存在 `left` 状态，但系统没有显式 leave 语义

问题表现：

- 数据模型已有 `MembershipState::Left`
- 运行时只有 `add_member / remove_member`
- 接入层没有标准化的 self leave 接口

根因：

- `55-成员治理与Direct会话成员边界标准-2026-04-06.md` 只冻结了治理矩阵，没有冻结成员主动离开的生命周期语义
- `conversation-runtime / projection-service / sdkwork-im-server` 只实现了 `joined / removed` 两条分支

本轮标准与修复：

- 新增 `docs/架构/56-成员主动离开会话与left状态标准-2026-04-06.md`
- 新增标准接口 `POST /im/v3/api/chat/conversations/{id}/members/leave`
- `group`
  - `admin / member / guest` 可 leave
  - `owner` 暂不允许 leave
- `direct`
  - 当前阶段统一拒绝 leave
- leave 统一提交 `conversation.member_left`
- leave 后立即从 active member 索引和投影中移除

代码落地：

- `services/conversation-runtime/src/lib.rs`
  - 新增 `ensure_member_leave_allowed(...)`
  - 完成 `leave_conversation(...)`
  - `build_member_envelope(...)` 支持 `conversation.member_left`
- `services/projection-service/src/lib.rs`
  - `apply(...)` 新增 `conversation.member_left`
  - 新增 `apply_member_left(...)`
- `crates/sdkwork-api-im-standalone-gateway/src/lib.rs`
  - 新增 `/members/leave`
  - 新增 `leave_conversation(...)`
  - 新增 `conversation.member_left` 审计锚点

新增验证：

- `services/conversation-runtime/tests/conversation_flow_test.rs`
  - `test_group_member_can_leave_and_loses_access`
  - `test_group_owner_cannot_leave_without_transfer`
  - `test_direct_conversation_rejects_leave_for_now`
- `services/conversation-runtime/tests/http_smoke_test.rs`
  - `test_group_member_can_leave_roster_over_http`
- `crates/sdkwork-api-im-standalone-gateway/tests/access_control_e2e_test.rs`
  - `test_group_member_can_leave_and_then_loses_conversation_access`

### 13.2 验证结果

已执行并通过：

- `cargo test -p conversation-runtime --test conversation_flow_test --offline`
- `cargo test -p conversation-runtime --test http_smoke_test --offline`
- `cargo test -p sdkwork-api-im-standalone-gateway --test access_control_e2e_test --offline`
- `cargo fmt --all`
- `cargo test -p conversation-runtime --offline`
- `cargo test -p projection-service --offline`
- `cargo test -p sdkwork-api-im-standalone-gateway --offline`
- `cargo test --workspace --offline`

## 14. 当前剩余风险

1. group owner transfer 仍未冻结，owner 暂时只能被保护性阻止 leave。
2. `left` 后重新加入的 member identity、cursor 与审计语义仍未冻结。
3. `agent_dialog / agent_handoff / system_channel` 仍未冻结成员生命周期标准。

## 15. 下一轮计划更新

1. 冻结 owner transfer 标准，解决 group owner 退出与主责任转移问题。
2. 冻结 `left -> rejoin` 标准，明确成员历史与新会话资格的关系。
3. 继续 review 其他会话类型的成员生命周期边界，避免通用 leave 接口被误用。

## 16. Review Cycle 5 实际完成结果

### 16.1 高风险：group owner 没有正式 transfer 机制，owner 生命周期被锁死

问题表现：

- 上一轮只做了“owner 不能 leave”
- 但系统没有正式的 owner transfer 命令、HTTP 契约、事件或审计锚点

根因：

- 前两轮只冻结了“治理矩阵”和“成员主动离开”
- 没有单独冻结 `group owner` 交接标准
- 代码层也没有 role mutation 的正式命令链路

本轮标准与修复：

- 新增 `docs/架构/57-group-owner-transfer标准-2026-04-06.md`
- 新增标准接口 `POST /im/v3/api/chat/conversations/{id}/members/transfer-owner`
- 仅 `group` 支持
- 仅当前 active `owner` 可以发起
- 目标必须是另一个 active member
- transfer 后：
  - 旧 owner -> `admin`
  - 新 owner -> `owner`
- transfer 统一提交 `conversation.owner_transferred`
- transfer 完成后，旧 owner 才可以继续执行 `leave`

代码落地：

- `services/conversation-runtime/src/lib.rs`
  - 新增 `TransferConversationOwnerCommand`
  - 新增 `TransferConversationOwnerPayload`
  - 新增 `TransferConversationOwnerResult`
  - 新增 `transfer_conversation_owner(...)`
  - 新增 `ensure_owner_transfer_allowed(...)`
  - 新增 `conversation.owner_transferred` envelope
- `crates/sdkwork-api-im-standalone-gateway/src/lib.rs`
  - 新增 `/members/transfer-owner`
  - 新增 `transfer_conversation_owner(...)`
  - 新增 `conversation.owner_transferred` 审计锚点

新增验证：

- `services/conversation-runtime/tests/conversation_flow_test.rs`
  - `test_group_owner_can_transfer_ownership_and_then_leave`
  - `test_group_admin_cannot_transfer_ownership`
  - `test_direct_conversation_rejects_owner_transfer`
- `services/conversation-runtime/tests/http_smoke_test.rs`
  - `test_group_owner_transfer_over_http`
- `crates/sdkwork-api-im-standalone-gateway/tests/access_control_e2e_test.rs`
  - `test_group_owner_transfer_allows_safe_handoff_and_leave`

### 16.2 验证结果

已执行并通过：

- `cargo test -p conversation-runtime --test conversation_flow_test --offline`
- `cargo test -p conversation-runtime --test http_smoke_test --offline`
- `cargo test -p sdkwork-api-im-standalone-gateway --test access_control_e2e_test --offline`

本轮完成后继续纳入格式化、模块级和全工作区离线回归。

## 17. 当前剩余风险

1. `left -> rejoin` 仍未冻结，成员身份、cursor 与历史审计关系未明确。
2. promote / demote 尚未标准化，目前只支持 owner handoff，不支持通用角色治理。
3. `agent_dialog / agent_handoff / system_channel` 的成员生命周期边界仍未冻结。

## 18. 下一轮计划更新

1. 冻结 `left -> rejoin` 标准，明确是复用历史成员还是生成新成员身份。
2. 评估是否需要独立的 promote / demote 标准，而不是继续扩张 owner transfer。
3. 继续 review 特殊会话类型的成员生命周期与治理边界。

## 19. Review Cycle 6 实际完成结果

### 19.1 重要：`left -> rejoin` 未冻结导致 membership identity 与 read cursor 漂移

问题表现：

- 同一 principal 在 `left` 后重新通过 `add_member` 加入时，运行时此前复用了同一个 `memberId`
- `conversation-runtime` 会按这个 `memberId` 直接覆盖 read cursor
- `projection-service` 对同一个 `memberId` 只做 `or_insert`，不会重置 cursor
- 结果是 runtime 与 projection 对重入成员的已读状态可能产生漂移

根因：

- `memberId` 此前只由 `conversationId + principalId` 决定，没有 membership episode 概念
- `add_member(...)` 只阻止“仍然 active 的同 principal”，不会为历史 episode 生成新身份
- `remove_member(...)` 也没有防御 stale `memberId`，在保留历史成员记录后会存在误删当前 active 映射的风险

本轮标准与修复：

- 新增 `docs/架构/58-left-rejoin-membership-episode标准-2026-04-06.md`
- 冻结规则：
  - `left` 或 `removed` 之后再次通过 `add_member` 加入，必须创建新的 membership episode
  - 新 episode 必须生成新的 `memberId`
  - 历史 episode 保留原始状态与 cursor，不覆盖
  - `GET/POST /read-cursor` 只面向当前 active episode
  - stale `memberId` 不得再操作当前 active member
- 代码落地：
  - `services/conversation-runtime/src/lib.rs`
    - 新增 `next_member_episode(...)`
    - 新增 `member_episode_id(...)`
    - 新增 `ensure_current_active_member_target(...)`
    - `add_member(...)` 改为按 episode 生成新 `memberId`
    - `remove_member(...)` 改为拒绝 stale `memberId`
- 新增验证：
  - `services/conversation-runtime/tests/conversation_flow_test.rs`
    - `test_left_member_rejoin_creates_new_membership_episode`
    - `test_stale_member_id_cannot_remove_rejoined_active_member`
  - `crates/sdkwork-api-im-standalone-gateway/tests/access_control_e2e_test.rs`
    - `test_left_member_rejoin_gets_new_member_identity_and_fresh_cursor`

### 19.2 验证结果

已执行并通过：

- `cargo fmt --all`
- `cargo test -p conversation-runtime --test conversation_flow_test --offline`
- `cargo test -p conversation-runtime --offline`
- `cargo test -p sdkwork-api-im-standalone-gateway --test access_control_e2e_test --offline`
- `cargo test -p sdkwork-api-im-standalone-gateway --offline`
- `cargo test --workspace --offline`

## 20. 当前剩余风险

1. promote / demote 仍未标准化，目前只支持 owner handoff，不支持通用角色治理。
2. `agent_dialog / agent_handoff / system_channel` 的成员生命周期边界仍未冻结。
3. 历史 membership episode 当前只做保留，不支持按 episode 查询审计视图与游标回放视图。

## 21. 下一轮计划更新

1. 评估是否需要独立的 promote / demote 标准，而不是继续扩张 owner transfer。
2. 继续 review 特殊会话类型的成员生命周期与治理边界。
3. 评估是否要补充面向运维与审计的 membership episode 查询视图，但不改变当前写模型。

## 22. Review Cycle 7 实际完成结果

### 22.1 重要：缺少通用 promote / demote 链路，群角色治理仍然不完整

问题表现：

- 当前只有特例化的 `owner transfer`，没有通用的 `admin / member / guest` 角色变更命令。
- 这意味着 owner 无法在不触发 owner handoff 的前提下，完成管理员授予、管理员降级、成员与访客之间的角色切换。
- 同时新引入的 membership episode 标准也要求任何新的治理命令都必须防御 stale `memberId`。

根因：

- 代码层此前没有正式的 role mutation 命令、接口、事件和审计动作。
- `owner transfer` 只解决 owner 交接，不适合作为通用角色治理通道。
- projection 读模型此前也没有成员 role 变更事件的应用逻辑。

本轮标准与修复：

- 新增 `docs/架构/59-group-member-role-change标准-2026-04-06.md`
- 冻结规则：
  - 新增 `POST /im/v3/api/chat/conversations/{id}/members/change-role`
  - 仅 `group` 支持
  - 仅当前 active `owner` 可发起
  - 目标必须是当前 active non-owner member
  - 目标角色只允许变更为 `admin / member / guest`
  - 涉及 `owner` 的角色变化必须通过 `transfer-owner`
  - stale `memberId` 必须被拒绝
- 代码落地：
  - `services/conversation-runtime/src/lib.rs`
    - 新增 `ChangeConversationMemberRoleCommand`
    - 新增 `ChangeConversationMemberRolePayload`
    - 新增 `ChangeConversationMemberRoleResult`
    - 新增 `change_conversation_member_role(...)`
    - 新增 `ensure_member_role_change_allowed(...)`
    - 新增 `conversation.member_role_changed` envelope
    - 新增 runtime HTTP 路由 `/members/change-role`
  - `crates/sdkwork-api-im-standalone-gateway/src/lib.rs`
    - 新增 app 路由 `/members/change-role`
    - 新增角色变更 handler
    - 新增 `conversation.member_role_changed` 审计锚点
  - `services/projection-service/src/lib.rs`
    - 新增 `conversation.member_role_changed` 事件应用
    - 新增成员快照查询能力，确保读模型跟随角色变更更新
- 新增验证：
  - `services/conversation-runtime/tests/conversation_flow_test.rs`
    - `test_group_owner_can_change_non_owner_member_roles`
    - `test_group_admin_cannot_change_member_roles`
    - `test_group_role_change_rejects_owner_target_and_direct_conversation`
    - `test_stale_member_id_cannot_change_rejoined_member_role`
  - `services/conversation-runtime/tests/http_smoke_test.rs`
    - `test_change_member_role_over_http`
  - `services/projection-service/tests/timeline_projection_test.rs`
    - `test_member_role_changed_event_updates_member_snapshot`
  - `crates/sdkwork-api-im-standalone-gateway/tests/access_control_e2e_test.rs`
    - `test_group_role_change_requires_owner_and_updates_target_role`

### 22.2 验证结果

已执行并通过：

- `cargo fmt --all`
- `cargo test -p conversation-runtime --test conversation_flow_test --offline`
- `cargo test -p conversation-runtime --test http_smoke_test --offline`
- `cargo test -p projection-service --test timeline_projection_test --offline`
- `cargo test -p sdkwork-api-im-standalone-gateway --test access_control_e2e_test --offline`
- `cargo test -p conversation-runtime --offline`
- `cargo test -p projection-service --offline`
- `cargo test -p sdkwork-api-im-standalone-gateway --offline`
- `cargo test --workspace --offline`

## 23. 当前剩余风险

1. `agent_dialog / agent_handoff / system_channel` 的成员生命周期边界仍未冻结。
2. 历史 membership episode 目前只做保留，不支持按 episode 查询审计视图与游标回放视图。
3. 通用角色治理当前只覆盖 group conversation，特殊会话类型尚未定义是否允许角色治理。

## 24. 下一轮计划更新

1. 继续 review 特殊会话类型的成员生命周期与治理边界。
2. 评估是否补充 membership episode 的只读审计/查询视图，但不改变当前写模型。
3. 评估是否需要把成员治理事件进一步对齐到更明确的 moderation / policy / automation 语义。

## 25. Review Cycle 8：特殊会话创建入口标准冻结（2026-04-06）

### 25.1 新发现

1. `conversation_type` 在 create-time 没有白名单校验，未知类型可以直接创建成功。
2. 创建链路丢失了认证上下文中的 `actor_kind`，导致所有创建者成员都被错误写成 `user`。
3. 这个问题会直接污染 `system_channel / agent_dialog / agent_handoff` 的成员模型、投影与审计元数据。

### 25.2 已实施修复

- `conversation-runtime`
  - create-time 白名单校验，只允许：
    - `group`
    - `direct`
    - `agent_dialog`
    - `agent_handoff`
    - `system_channel`
  - 新增 `conversation_type_invalid`
  - 创建事件与首个成员记录都改为使用真实 `actor_kind`
- `sdkwork-im-server`
  - 创建入口改为把认证上下文中的 `actor_kind` 透传到 runtime
  - 同步补齐 `conversation_type_invalid` 错误映射

### 25.3 已补充测试

- `test_create_conversation_rejects_unknown_conversation_type`
- `test_create_conversation_rejects_unknown_type_over_http`
- `test_create_system_channel_preserves_actor_kind_over_http`
- `test_system_channel_create_preserves_actor_kind_in_local_profile`

### 25.4 当前剩余风险

1. `agent_dialog / agent_handoff / system_channel` 的后续生命周期命令仍未冻结。
2. 特殊类型虽然已在创建边界收敛，但 generic member governance 仍然只能保持关闭。
3. membership episode 的审计查询视图仍未补齐。

### 25.5 下一轮计划

1. 冻结 `agent_dialog / agent_handoff / system_channel` 的成员生命周期矩阵。
2. 判断是否需要 special conversation dedicated create payload。
3. 继续 review policy / moderation / automation 对特殊会话类型的差异化约束。

## 26. Review Cycle 9：special conversation generic create 收口（2026-04-06）

### 26.1 新发现

1. 上一轮虽然把 special type 的 create-time 类型校验和 `actor_kind` 透传补齐了，但 generic create 仍然允许 `agent_dialog / agent_handoff / system_channel` 直接落库。
2. 这些类型当前没有 dedicated create payload，也没有专用生命周期命令，generic create 会生成语义残缺的半成品会话。
3. 这不是功能缺失的小问题，而是公共写路径边界定义错误。

### 26.2 已实施修复

- `conversation-runtime`
  - generic create 现在只允许 `group / direct`
  - `agent_dialog / agent_handoff / system_channel` 改为保留类型，统一返回 `conversation_type_invalid`
- 已保留上一轮的正确改动：
  - supported generic create 仍然透传真实 `actor_kind`
  - app/local profile 仍然保持一致错误映射

### 26.3 已补充测试

- `test_generic_create_rejects_unknown_and_reserved_special_conversation_types`
- `test_generic_create_rejects_reserved_special_types_over_http`
- `test_generic_create_rejects_reserved_special_types_in_local_profile`
- `test_group_create_preserves_actor_kind_over_http`

### 26.4 当前剩余风险

1. `agent_dialog / agent_handoff / system_channel` 的 dedicated create command 仍未定义。
2. 这些类型目前只存在于模型标准层，还没有安全的公共写路径。
3. membership episode 的审计/查询视图仍然是后续波次。

### 26.5 下一轮计划

1. 为 `agent_dialog / agent_handoff / system_channel` 分别定义专用 create contract。
2. 先冻结每类 special conversation 的参与者拓扑，再开放写路径。
3. 对齐 dedicated create 与 policy / moderation / audit 的边界。

## 27. Review Cycle 10：agent_dialog 专用创建落地（2026-04-06）

### 27.1 新发现

1. 上一轮已经把 generic create 收口到 `group / direct`，但 `agent_dialog` 仍然没有一条安全的公共写路径。
2. 如果不限制创建者主体类型，`system -> agent` 也会被错误建模成 `agent_dialog`，污染 special conversation 语义。
3. 这是 special conversation 第一条 dedicated create 垂直切片，必须先冻结请求者、目标 agent、成员拓扑与游标初始化标准。

### 27.2 已实施修复

- `conversation-runtime`
  - 新增 `CreateAgentDialogCommand`
  - 新增 `create_agent_dialog_with_requester_kind(...)`
  - 仅允许 `requester_kind=user`
  - 创建后一次性落下 requester + agent 两个 active member
  - 为两个成员同时初始化 read cursor
- `sdkwork-im-server`
  - 新增 `POST /im/v3/api/chat/conversations/agent_dialogs`
  - 继续坚持 requester 身份只来自认证上下文，而不是请求体

### 27.3 已补充测试

- `test_create_agent_dialog_creates_requester_and_agent_members`
- `test_create_agent_dialog_rejects_non_user_requester_kind`
- `test_create_agent_dialog_over_http`
- `test_create_agent_dialog_rejects_non_user_actor_over_http`
- `test_agent_dialog_create_in_local_profile_creates_user_and_agent_members`
- `test_agent_dialog_create_rejects_non_user_creator_in_local_profile`

### 27.4 当前剩余风险

1. `agent_handoff` 仍未定义 dedicated create contract。
2. `system_channel` 仍未定义 dedicated create contract。
3. 部分成员治理事件的 actor metadata 仍待下一轮审计硬化。

### 27.5 下一轮计划

1. 为 `system_channel` 冻结 system-only dedicated create 与订阅模型。
2. 为 `agent_handoff` 冻结 source/target/handoff metadata 与写路径。
3. 对齐成员治理事件中的 actor identity，避免 create 之外的 mutation 审计语义继续简化。

