> Migrated from `docs/review/S05-Loop42补充-2026-04-11.md` on 2026-06-24.
> Owner: SDKWork maintainers

# S05 Loop42补充 - 2026-04-11

## 1. 本轮主任务
- 关闭 `S05` 在 social `journal + snapshot` 写路径上缺少显式多文件事务边界的主缺口。

## 2. 已兑现事实
- `services/control-plane-api/src/lib.rs` 新增 `state/social-transaction-marker.json`，把 `journal append` 已成功但 `snapshot save` 尚未确认成功的窗口显式物化为 pending marker。
- runtime-dir social 写路径现为 `append social-commit-journal -> write social-transaction-marker -> save social-state -> clear marker`；当 `snapshot save` 失败时，marker 会保留下来，避免跨进程重启后只剩“snapshot 落后”而缺少事务状态证据。
- startup journal replay、same-event retry 的 best-effort snapshot repair、HTTP `POST /backend/v3/api/control/social/runtime/repair-derived-snapshot` 与 standalone `control-plane-api repair-social-runtime-dir --runtime-dir <path> [--json]` 成功后都会清除 marker，避免 repaired snapshot 仍背着 stale pending state。
- 新增并保持通过：
  - `test_control_plane_social_file_runtime_leaves_pending_tx_marker_after_snapshot_failure_and_clears_it_after_restart_replay`
- fresh 验证：
  - `cargo test -p control-plane-api --offline --test social_friend_request_test test_control_plane_social_file_runtime_leaves_pending_tx_marker_after_snapshot_failure_and_clears_it_after_restart_replay -- --nocapture` = `passed`
  - `cargo fmt -p control-plane-api -p sdkwork-im-server` = `passed`
  - `cargo test -p control-plane-api --offline --tests -- --nocapture` = `61 passed`
  - `cargo test -p sdkwork-api-im-standalone-gateway --offline --test deployment_profile_test -- --nocapture` = `64 passed`

## 3. 仍未关闭
- `S05` 仍未达到 `step_closure`
- 当前只形成了 `repair-marker based atomic multi-file tx minimal boundary`，尚未宣称更强的 `staged / manifest` 级事务
- `repair-runtime-local.* --json` 仍保留 generic repair 的 stdout 合同，本轮未扩展 wrapper-level 聚合 JSON envelope

## 4. 当前结论
- Loop42 把 `S05` 从“journal-first 但无显式多文件事务边界”推进到“有 durable pending marker、可在 restart/retry/repair 后清理的最小事务边界”。`S05` 因 `step_closure` 证据仍未齐备，只能继续标记为 `not_closed / local_closure`。

