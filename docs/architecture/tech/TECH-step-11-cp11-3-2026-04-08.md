> Migrated from `docs/review/step-11-cp11-3-高可用与恢复演练基线-执行卡-2026-04-08.md` on 2026-06-24.
> Owner: SDKWork maintainers

# Step 11 / CP11-3 高可用与恢复演练基线 执行卡 - 2026-04-08

## 当前上下文
- 当前波次：`Wave D`
- 当前 step：`Step 11`
- 当前子任务：`CP11-3`
- 前置状态：
  - `CP11-1` 已冻结场景清单
  - `CP11-2` 已完成第一轮 `connection / message / stream` 定量结果
  - 当前真实缺口收敛到：必须拿出 `drain / rebalance / restore / failover` 的真实演练结果

## 本轮为什么做这个增量
- `docs/step/11-性能高可用与灾备演练.md` 要求 `CP11-3` 完成 `drain / rebalance / restore / failover` 演练。
- 当前仓库已有 route drain、runtime restore、cross-node resume takeover 的基础资产，因此本轮最优决策是把它们编排成一条可重复执行的本地 drill baseline，而不是新增一套独立 chaos 框架。

## 本轮实际完成

### 1. 已建立可重复执行的本地 drill baseline
- 新增：`tools/perf/step-11-cp11-3-local-drill-baseline.json`
- 当前冻结参数：
  - `expectedRouteCount = 1`
  - `expectedRestoredFileCount = 11`
  - `expectedOwnerNodeId = node_b`
- 基线层级：
  - `profile = standalone.split-services.development`
  - `tier = CI Smoke Tier`

### 2. 已新增真实 HA/DR drill 门禁
- 新增：`crates/sdkwork-api-im-standalone-gateway/tests/performance_ha_dr_drill_test.rs`
- 覆盖：
  - `drain-rebalance`
  - `restore-recovery`
  - `failover`
- 每个场景都会打印一条 `STEP11_DRILL {json}` 指标行，便于 review 文档直接回填结果

### 3. 已把本地 drill baseline 接入 Step 11 文档与 catalog
- `docs/部署/性能与灾备演练场景.md`
  - 新增 `tools/perf/step-11-cp11-3-local-drill-baseline.json`
  - 新增 `crates/sdkwork-api-im-standalone-gateway/tests/performance_ha_dr_drill_test.rs`
- `tools/perf/step-11-scenario-catalog.json`
  - `drain-rebalance`
  - `restore-recovery`
  两类场景已从 `catalog-frozen` 推进到 `measured-local-drill`

### 4. 已完成第一轮本地演练结果
- `drain-rebalance`
  - `expectedRouteCount = 1`
  - `migratedRouteCount = 1`
  - `deliveredEventCount = 1`
  - `drillDurationMs = 0.661`
- `restore-recovery`
  - `expectedRestoredFileCount = 11`
  - `restoredFileCount = 11`
  - `previewDurationMs = 2.159`
  - `restoreDurationMs = 18.313`
- `failover`
  - `expectedOwnerNodeId = node_b`
  - `activeOwnerNodeId = node_b`
  - `staleDisconnectCode = stale_session`
  - `takeoverDurationMs = 0.438`

## TDD / Red-Green 证据

### Red
- `cargo test -p sdkwork-api-im-standalone-gateway --offline --test performance_ha_dr_drill_test`
  - 初始失败：缺少 `tools/perf/step-11-cp11-3-local-drill-baseline.json`

### Green
- 补齐本地 drill baseline 配置、文档接线与演练测试后，同一套测试 fresh 保持通过
- 并通过 `-- --nocapture` 输出真实演练指标，不是静态文档填数

## Fresh 验证
- `cargo test -p sdkwork-api-im-standalone-gateway --offline --test performance_ha_dr_drill_test -- --nocapture`
- `cargo fmt --all --check`

## 当前判断
- `CP11-3`：闭环
- 已兑现：
  - drain / rebalance 已有真实演练结果
  - restore 已有真实演练结果
  - failover 已有最小可信的跨节点 resume takeover 演练结果
- 当前仍未兑现：
  - `upgrade-rollback` 的 Step 11 演练证据
  - `CP11-4` 的整步量化报告与收口结论

## 下一轮继续做什么
1. 进入 `CP11-4`
2. 汇总 `CP11-2 / CP11-3` 的量化与演练结果，补齐整步性能 / HA / DR 报告
3. 决定 `upgrade-rollback` 是否作为 Step 11 最后缺口单独补齐，还是明确记为当前未兑现项

