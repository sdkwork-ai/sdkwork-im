> Migrated from `docs/架构/152CJ-Loop21补充-2026-04-10.md` on 2026-06-24.
> Owner: SDKWork maintainers

# 152CJ Loop-21 补充

## 本轮新增事实
- `automation-service` 新增 5 个 S11 HTTP 主链入口：
  - `POST /im/v3/api/automation/agent-responses`
  - `POST /im/v3/api/automation/agent-responses/{stream_id}/frames`
  - `POST /im/v3/api/automation/agent-responses/{stream_id}/complete`
  - `POST /im/v3/api/automation/agent-tool-calls`
  - `POST /im/v3/api/automation/executions/{execution_id}/agent-tool-calls/{tool_call_id}/complete`
- `sdkwork-im-server` 镜像同一 assembled surface，并把 agent response/tool call 生命周期写入 audit anchor。
- 事实口径已从“runtime 内部可用、HTTP 缺口存在”升级为“standalone + assembled HTTP 主链均可验证”。

## 本轮未宣称
- 不宣称 `S11 = step_closure`。
- 不宣称 `automation guardrail / operator override / public governance surface` 已闭环。
- 不宣称 `S12` 的 `external_connection / external_member_link / shared_channel_policy` 已成为实现事实。

## Fresh Verification
- `cargo test -p automation-service --offline --tests`
- `cargo test -p sdkwork-api-im-standalone-gateway --offline --test task10_capabilities_e2e_test`
- `cargo test -p sdkwork-api-im-standalone-gateway --offline --test websocket_e2e_test test_local_minimal_profile_pushes_agent_handoff_lifecycle_events_over_websocket`
- `cargo test -p sdkwork-api-im-standalone-gateway --offline --test access_control_e2e_test test_agent_handoff_accept_resolve_close_in_local_profile`

## 结论
- `S11` 提升为 `local_closure = agent lifecycle HTTP surface + assembled audit anchors`。
- 下一轮继续 `S11`，主缺口收敛到 `CPR11-3 = guardrail / operator override`。

