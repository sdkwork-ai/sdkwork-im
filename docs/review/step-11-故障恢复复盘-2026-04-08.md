# Step 11 故障恢复复盘 - 2026-04-08

## 环境说明
- 波次 / Step：`Wave D / Step 11 / CP11-3`
- 执行基线：
  - `profile = standalone.development`
  - `tier = CI Smoke Tier`
- 执行入口：
  - `tools/perf/step-11-cp11-3-local-drill-baseline.json`
  - `crates/sdkwork-api-im-standalone-gateway/tests/performance_ha_dr_drill_test.rs`
- 执行命令：
  - `cargo test -p sdkwork-api-im-standalone-gateway --offline --test performance_ha_dr_drill_test -- --nocapture`

## 演练结果

| Drill | 结果 | 关键指标 |
| --- | --- | --- |
| `drain-rebalance` | 通过 | `migratedRouteCount = 1`，`deliveredEventCount = 1`，`drillDurationMs = 0.983` |
| `restore-recovery` | 通过 | `restoredFileCount = 11`，`previewDurationMs = 2.453`，`restoreDurationMs = 17.983` |
| `failover` | 通过 | `activeOwnerNodeId = node_b`，`staleDisconnectCode = stale_session`，`takeoverDurationMs = 0.553` |
| `upgrade-rollback` | 通过 | `compatibleClientCount = 4 / 4`，`rollbackActivationMs = 0.007`，`postRollbackProtocolErrorRate = 0.0` |

## 异常点
- 当前 `drain-rebalance` 和 `failover` 结果都基于单 route / 单 device 场景，不能外推为大规模多租户环境结论。
- 当前 `restore-recovery` 仍是本地 runtime-dir snapshot 路径，不代表 object storage / archive / cross-region 恢复已经完成。
- 当前 `upgrade-rollback` 只验证最小可信的 kill switch rollback 路径，不代表多 cell / 多 region 的真实编排已经完成。

## 当前结论
- `CP11-3` 已满足最小本地演练要求：
  - drain / rebalance
  - restore
  - failover
- `CP11-4` 已把 `upgrade-rollback` 缺口补齐到可重复执行的本地 drill：
  - control-plane governance snapshot 与 runtime hello 协商现在拥有同一条 rollback 证据链
- 当前不应得出的结论：
  - 多 cell / region 灾备切换已完成
  - 多 cell / region 的升级编排已经完成

## 下一步建议
1. 保留当前本地 drill 基线，作为后续更高 tier HA / DR / rollback 演练的最低回归门禁。
2. 自动进入 `Step 12`，把 CLI / SDK / compatibility matrix 的收口建立在 Step 11 已冻结的运行与治理基线之上。
