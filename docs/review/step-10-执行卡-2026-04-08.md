# Step 10 执行卡 - 2026-04-08

## 当前上下文
- 当前波次：`Wave D`
- 当前 step：`Step 10`
- 子任务状态：
  - `CP10-1`：统一命令面冻结完成
  - `CP10-2`：多环境 profile / template 合同冻结完成
  - `CP10-3`：repeatable smoke 与 public signed bearer 合同完成
  - `CP10-4`：runtime ops profile-aware 运维闭环完成
- 当前需要判定的是：`Step 10` 是否已满足整步 `91 / 95 / 97`，并允许进入 `Step 11`

## 本轮为什么做整步审计
- `docs/step/10-部署脚本与多环境发布治理.md` 的完成定义要求：
  - 部署脚本、配置模板与 profile 已形成统一交付体系
  - 本地安装、启动、状态、停止、恢复可重复执行
  - 交付能力不再依赖口头说明
- 经过 `CP10-1 ~ CP10-4` 的连续增量，当前仓库已具备相应代码、脚本、文档与自动化证据，因此需要从子任务完成提升到整步闭环判断。

## Step 10 整步实际完成

### 1. 统一交付入口已形成真实命令矩阵
- `bin/` 命令面已冻结：
  - `retired-lifecycle-install`
  - `init-config-local`
  - `retired-lifecycle-start`
  - `retired-lifecycle-status`
  - `retired-lifecycle-restart`
  - `retired-lifecycle-stop`
  - `retired-lifecycle-deploy`
  - `open-chat-test`
- PowerShell / CMD / Bash 已共享同一组文档化参数语义，而不是平台各写各的入口

### 2. 多环境 profile / template 已从命名走到可执行合同
- `standalone.development` 与 `standalone.development` 已具备：
  - compose profile 入口
  - env template
  - `retired-lifecycle-deploy` profile selector
  - runtime ops profile selector
- 当前 `standalone.development` 仍复用 `standalone.development` 服务/运行合同，这一点已被脚本、文档与测试同时冻结

### 3. 部署后 smoke / public auth 已可重复执行
- `retired-lifecycle-deploy` 现已支持显式 smoke base-url
- Docker `standalone.development` compose 已注入 public bearer dev secret
- `tools/smoke/local_stack_smoke.ps1` / `.sh` 已基于 HS256 bearer 对 `build_public_app()` 执行真实 smoke

### 4. 恢复、修复、检查脚本已纳入标准运维闭环
- inspect / repair / list backups / archive / prune / preview restore / restore 已形成完整 runtime ops 面
- 统一支持：
  - PowerShell / CMD：`-ProfileName <standalone.development|standalone.development>`
  - Bash：`--profile <standalone.development|standalone.development>`
- `standalone.development` 运维路径已遵守 config-first / runtime-contract fallback 合同

### 5. operator 文档已不再落后于真实脚本
- `docs/部署/快速启动脚本.md`
- `docs/部署/本地最小安装与运行.md`
- `docs/部署/README.md`
- `docs/部署/多环境Profile与配置模板.md`
- `README.md`
- 上述文档已覆盖 profile、smoke、public auth、runtime ops 的真实使用口径

## Step 10 验证证据
- `cargo fmt --all --check`
- `cargo test -p sdkwork-api-im-standalone-gateway --offline --test deployment_profile_test`
- `cargo test -p sdkwork-api-im-standalone-gateway --offline --test public_auth_e2e_test`
- `powershell -NoProfile -ExecutionPolicy Bypass -File pnpm dev -Help`
- `powershell -NoProfile -ExecutionPolicy Bypass -File bin/inspect-runtime-local.ps1 -Help`
- `powershell -NoProfile -ExecutionPolicy Bypass -File bin/repair-runtime-local.ps1 -Help`
- `powershell -NoProfile -ExecutionPolicy Bypass -File bin/list-runtime-backups-local.ps1 -Help`
- `powershell -NoProfile -ExecutionPolicy Bypass -File bin/archive-runtime-backup-local.ps1 -Help`
- `powershell -NoProfile -ExecutionPolicy Bypass -File bin/prune-runtime-archives-local.ps1 -Help`
- `powershell -NoProfile -ExecutionPolicy Bypass -File bin/preview-runtime-restore-local.ps1 -Help`
- `powershell -NoProfile -ExecutionPolicy Bypass -File bin/restore-runtime-local.ps1 -Help`
- `cmd /c bin\\retired-lifecycle-deploy.cmd --help`
- `cmd /c bin\\inspect-runtime-local.cmd --help`

## 结论
- `91`：通过
- `95`：通过
- `97`：已完成
- `Step 10`：闭环完成

## 当前还差什么
- `Step 11` 的压测、故障演练与灾备验证尚未开始
- `Wave D` 尚未闭环，仍不能执行 `93`

## 下一轮继续做什么
1. 自动进入 `Wave D / Step 11`
2. 先对 `Step 11` 的压测 / 故障演练 / 灾备验证准入条件做真实状态核对
3. 不回退 `Step 10` 刚冻结的命令、profile、smoke 与 runtime ops 合同
