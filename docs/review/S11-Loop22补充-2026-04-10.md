# S11 Loop-22 补充

## 本轮新增事实
- `automation-service` 新增 `GET /im/v3/api/automation/governance`，公开最小治理快照：`capabilityProfileId / enabledCapabilities / guardrailPolicyId / restrictedToolPrefixes / operatorOverridePermission / operatorOverrideActive`。
- runtime 为受限 tool 前缀 `ops.`、`admin.` 实施 guardrail；无 `automation.operator_override` 时拒绝并写入 `automation.guardrail_denied` 事件。
- 具备 `automation.operator_override` 时，允许受限 tool call，并追加 `automation.operator_override_applied` 事件。
- `sdkwork-im-server` 镜像 governance surface，并把 `automation.guardrail_denied / automation.operator_override_applied` 写入 assembled audit anchor。

## Fresh Verification
- `cargo test -p automation-service --offline test_restricted_tool_call_requires_operator_override_and_is_auditable -- --nocapture`
- `cargo test -p automation-service --offline test_automation_governance_surface_and_operator_override_over_http -- --nocapture`
- `cargo test -p sdkwork-api-im-standalone-gateway --offline test_local_minimal_profile_exposes_automation_governance_and_override_audit -- --nocapture`
- `cargo test -p automation-service --offline --tests`
- `cargo test -p sdkwork-api-im-standalone-gateway --offline --test task10_capabilities_e2e_test`

## 结论
- `CPR11-3` 已闭环。
- `S11 = step_closure`。
- 当前闭环语义是“静态 guardrail policy + permission-derived operator override + public read surface”；控制面写入/版本化不在本轮宣称范围内。
