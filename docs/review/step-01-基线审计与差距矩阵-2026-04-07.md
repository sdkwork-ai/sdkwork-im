# Step 01 基线审计与差距矩阵（2026-04-07）

## 1. 审计范围

- `docs/架构/README.md`
- `docs/架构/09-实施计划.md`
- `docs/架构/130-149` 中与波次 A 直接相关文档
- `docs/step/90-架构能力-Step-代码目录-证据映射矩阵.md`
- 当前 workspace 目录与高风险文件
- 当前 `git status`
- 当前 `docs/review` 目录

## 2. 当前仓库真实状态

| 项 | 状态 | 证据 |
| --- | --- | --- |
| workspace 基础目录 | 已存在 | 根目录已有 `crates/`、`services/`、`bin/`、`deployments/`、`tools/`、`sdks/`、`adapters/` |
| Step 治理文档 | 已存在但此前未归档执行证据 | `docs/step/00-13`、`90-98` |
| Step review 治理证据 | 本轮前缺失 | `docs/review` 中原本无 `step-00/01/02` 文档 |
| `session-gateway` 文件治理 | 部分落地 | `src/lib.rs` 已降至 `601` 行，新增 `src/presence.rs` 与结构测试 |
| `conversation-runtime` 文件治理 | 部分落地 | `src/lib.rs` 已降到 `2` 行，HTTP 面已抽到 `src/runtime/http.rs`，但主实现 `src/runtime.rs` 仍为 `3103` 行 |
| `sdkwork-im-server` 文件治理 | 部分落地 | `src/lib.rs` 已降到 `2` 行，但主实现迁移到 `src/node.rs` 后仍为 `5214` 行 |
| `CCP / contract-*` crate 族 | 未按目标形态落地 | `docs/架构/147` 目标与当前代码仍有差距 |

## 3. 高风险文件清单

| 文件 | 当前风险 | 关联 step | 后续动作 |
| --- | --- | --- | --- |
| `crates/sdkwork-api-im-standalone-gateway/src/lib.rs` | 入口文件风险已收口，但真实实现已迁移到 `src/node.rs` | `01` `02` | Step 02 已完成 facade 收口，后续继续治理 `src/node.rs` |
| `crates/sdkwork-api-im-standalone-gateway/src/node.rs` | 装配、运行时、HTTP 接口与脚本相关能力仍集中在单文件 | `02` `10` | 继续在 Step 02/10 拆分实现文件 |
| `services/conversation-runtime/src/lib.rs` | 入口文件风险已收口，但真实实现已迁移到 `src/runtime.rs` | `01` `02` `05` | Step 02 已完成 facade 收口，后续继续治理 `src/runtime.rs` |
| `services/conversation-runtime/src/runtime.rs` | 消息主链路、恢复逻辑与策略辅助逻辑仍集中在单文件 | `02` `05` | 继续在 Step 02/05 拆分实现文件 |
| `services/session-gateway/src/lib.rs` | 连接热路径与 presence/runtime 曾混写 | `01` `02` `03` `04` | 已完成首轮拆分，后续继续抽连接热路径 |

## 4. 差距矩阵

| 目标能力 | 目标文档 | 当前状态 | 结论 |
| --- | --- | --- | --- |
| workspace / crate 骨架可承载后续重构 | `09` `133` `147` | 部分达成，目录骨架已在，边界仍需继续收口 | 进入 `Step 02` 持续治理 |
| 超大入口文件治理 | `09` `133` | 三个高风险 `lib.rs` 已收口，但 `runtime.rs/node.rs` 仍超长 | `Step 02` 未闭环 |
| `CCP / contract-*` crate 族落地 | `147` | 尚未开始成体系落地 | 只能在 `Step 03` 继续，当前不能跳过 |
| 波次 A 门禁 | `93` | `00-03` 未全部通过 | 不能进入波次 B |

## 5. Step 映射结论

- `Step 00`：本轮已补齐执行治理证据，可闭环。
- `Step 01`：本轮已完成真实基线冻结，可闭环。
- `Step 02`：已完成三处高风险 `lib.rs` 收口，但整体仍未闭环。
- `Step 03`：因 `Step 02` 未闭环且 `CCP / contract-*` 未落地，禁止提前进入。

## 6. 结论

当前仓库的真实推进位置不是“已进入主链路重构”，而是“波次 A 中 Step 02 仍在进行，Step 03 尚未开始”。
