# S13 Loop-26 补充

## 本轮新增事实
- deploy/profile/operator contract 已由 `deployment_profile_test` 证明，而非文档占位：
  - `standalone.development / standalone.development`
  - release bundle manifest / evidence root
  - operator post-release sample / record template
  - `inspect / repair / list / archive / prune / preview / restore` 统一 profile 入口
- runtime-dir 恢复链已由 fresh 测试串成可审计闭环：
  - backup catalog 暴露 `active / archived`
  - archive metadata 暴露 `storageClass / retentionPolicy / retentionDays / archivedAt / restoreStatus / legalHold`
  - prune 会删除到期归档并跳过 `legal_hold`
  - repair 采用 backup-first，保留损坏文件并补齐缺失文件
  - restore preview 提供 typed diff/fingerprint
  - restore 会创建 pre-restore backup，并阻断 fingerprint mismatch
- `CI Smoke Tier` 已有 2026-04-10 fresh real evidence：
  - connection：`connectP95Ms = 26.857`、`connectionsPerSecond = 1054.397`
  - message：`postP95Ms = 0.268`、`messageTps = 5719.648`
  - stream：`appendP95Ms = 0.220`、`framesPerSecond = 5555.507`
- `HA / DR / rollback` 已有 2026-04-10 fresh real evidence：
  - drain-rebalance：`drillDurationMs = 0.975`、`migratedRouteCount = 1`
  - failover：`takeoverDurationMs = 0.740`、`activeOwnerNodeId = node_b`
  - restore-recovery：`previewDurationMs = 2.992`、`restoreDurationMs = 27.017`、`restoredFileCount = 12`
  - upgrade-rollback：`compatibilityMatrixPassRate = 1.0`、`rollbackActivationMs = 0.007`、`postRollbackProtocolErrorRate = 0.0`
- higher tier 当前真相已 machine-readable：
  - `scenario catalog` 只冻结 scenario inventory 与 gate template；tier collection state 以 evidence index 为准
  - `Pre-Release Tier = evidence_collected_gate_blocked`，`collectedSlots = 7`
  - `Capacity Tier = template_only_pending_execution`，`collectedSlots = 0`

## 当前准确口径
- `CPR13-1 = yes`
- `CPR13-2 = yes`
- `CPR13-3 = yes`
- `CPR13-4 = yes`
- `S13 = step_closure`

## 结论边界
- 当前真实已闭环：
  - deploy/profile/operator/runtime-ops contract
  - runtime-dir backup/archive/prune/repair/preview/restore audit chain
  - `CI Smoke Tier` quant / drill / rollback fresh evidence
  - `Pre-Release Tier` machine-readable evidence index
  - `Capacity Tier` template/evidence-root contract
- 当前明确未闭环：
  - 真 `capacity-dedicated` 量化与恢复报告
  - 多 cell / 多 region / object storage / cross-region DR
  - 全量生产级 SLO 阈值、告警编排、自动 gate sign-off

## 验证
- `cargo test -p sdkwork-api-im-standalone-gateway --offline --test deployment_profile_test -- --nocapture`：`62 passed`
- `cargo test -p sdkwork-api-im-standalone-gateway --offline --test runtime_dir_backup_catalog_test -- --nocapture`：`4 passed`
- `cargo test -p sdkwork-api-im-standalone-gateway --offline --test runtime_dir_repair_test -- --nocapture`：`2 passed`
- `cargo test -p sdkwork-api-im-standalone-gateway --offline --test runtime_dir_restore_preview_test -- --nocapture`：`10 passed`
- `cargo test -p sdkwork-api-im-standalone-gateway --offline --test runtime_dir_restore_test -- --nocapture`：`3 passed`
- `cargo test -p sdkwork-api-im-standalone-gateway --offline --test performance_quant_baseline_test -- --nocapture`：`4 passed`
- `cargo test -p sdkwork-api-im-standalone-gateway --offline --test performance_ha_dr_drill_test -- --nocapture`：`5 passed`

## 下一轮入口
- 主脊切换到 `S14`。
- `S14` 只收口 SDK / CLI / release / loop protocol，不回滚当前 `S13` 的 tier 真值边界。
- 若后续补做 `capacity-dedicated` 或多 region DR，必须以新 evidence 和新 loop 回写，不得借用本轮 `S13` 结论。
