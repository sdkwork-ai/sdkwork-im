# S05 Loop43补充 - 2026-04-11

## 1. 本轮主任务
- 关闭 `repair-marker` 仍停留在实现内部、尚未进入可审计 operator surface 的缺口。

## 2. 已兑现事实
- `services/control-plane-api/src/lib.rs` 的 `SocialRuntimeRepairResponse` 新增 `transactionMarkerCleared`。
- `POST /backend/v3/api/control/social/runtime/repair-derived-snapshot` 与 `control-plane-api repair-social-runtime-dir --runtime-dir <path> [--json]` 现在都会显式返回本次修复是否清理了 pending transaction marker。
- 文本型 `repair-social-runtime-dir` 输出也新增 `transaction-marker-cleared: <bool>` 行，避免 operator 只能依赖文件系统侧推断 marker 是否被清掉。
- `services/control-plane-api/tests/social_runtime_cli_test.rs` 新增并保持通过：
  - `test_control_plane_repair_social_runtime_dir_cli_reports_transaction_marker_clearance_after_snapshot_failure`
- `services/control-plane-api/tests/social_friend_request_test.rs` 继续把同一字段锁到 HTTP repair：
  - failpoint 触发出的 pending marker 路径断言 `transactionMarkerCleared = true`
  - 外部 journal replay 但无 marker 的 repair 路径断言 `transactionMarkerCleared = false`
- fresh 验证：
  - `cargo test -p control-plane-api --offline --test social_runtime_cli_test test_control_plane_repair_social_runtime_dir_cli_reports_transaction_marker_clearance_after_snapshot_failure -- --nocapture` = `passed`
  - `cargo fmt -p control-plane-api -p sdkwork-im-server` = `passed`
  - `cargo test -p control-plane-api --offline --tests -- --nocapture` = `62 passed`
  - `cargo test -p sdkwork-api-im-standalone-gateway --offline --test deployment_profile_test -- --nocapture` = `64 passed`

## 3. 仍未关闭
- `S05` 仍未达到 `step_closure`
- 当前虽已具备 `repair-marker` 的最小事务边界与显式 operator surface，但尚未宣称更强 `staged / manifest` 级事务
- 本轮没有把 `repair-runtime-local.* --json` 扩展为聚合 social repair report 的 wrapper-level JSON envelope

## 4. 当前结论
- Loop43 把 `repair-marker` 从“实现内部的修复边界”推进到“operator 可直接观测的修复 contract”。`S05` 因 `step_closure` 证据仍未最终收口，继续标记为 `not_closed / local_closure`。
